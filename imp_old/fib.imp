(* computes the first 10 Fibonacci numbers, and writes them to a list *)
program 
const n = 10; array a[10]; var i
begin
	a[0] := 0;
	a[1] := 1;
	i := 2;
	while i<n do
	begin
		a[i] := a[i-1] + a[i-2];
		i := i+1
	end
end